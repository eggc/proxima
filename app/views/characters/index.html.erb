<%= form_with(method: :get) do |f| %>
  <%= f.select(:project_id, @selectable_projects, include_blank: true) %>
  <%= f.submit "Search" %>
<% end %>

<%= render("shared/character_table", characters: @characters, hidden_columns: []) %>

<%= link_to("new", new_character_path) %>
